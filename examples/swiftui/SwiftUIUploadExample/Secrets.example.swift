// Template — copy this file to `Secrets.swift` and paste your credentials.
//
//     cp Secrets.example.swift Secrets.swift
//
// `Secrets.swift` is gitignored so your keys never get committed.
// Get a token + secret key from https://dashboard.fastpix.com (Settings → Access Tokens).

enum Secrets {
    static let token = "YOUR_FASTPIX_TOKEN"
    static let secretKey = "YOUR_FASTPIX_SECRET_KEY"
}
